class ScifiCalcultorM extends Calculator
{
	double square(double a){ return a*a;}
	double cube(double a){ return a*a*a;}
	double squareRoot(double a){ return Math.sqrt(a);}
	double cubeRoot(double a){ return Math.cbrt(a);}
}
